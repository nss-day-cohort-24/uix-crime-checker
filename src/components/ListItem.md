``` js
<ListItem event_number={item.event_number}
                itemCategory={item.itemCategory}
                disposition_description={item.disposition_description}
                latitudes={item.latitude}
                description={item.description} />
```